#ifndef _FILTERING_BRAIN_HPP_
#define  _FILTERING_BRAIN_HPP_

/// A filter is a type
/// thats constraints
/// or transforms an
/// object with the
/// operator |
template<typename type_t>
struct filter
{
};



#endif
